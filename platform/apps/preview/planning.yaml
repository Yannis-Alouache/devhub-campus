# ApplicationSet preview pour planning.
# Meme strategie que pour `annuaire` : previews sur PR GitHub `feature/*`
# vers `main`, avec un token GitHub fourni par Secret pour eviter la
# limite anonyme de l'API.

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: planning-preview
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - pullRequest:
        requeueAfterSeconds: 180
        github:
          owner: Yannis-Alouache
          repo: devhub-campus
          tokenRef:
            secretName: github-token
            key: token
        filters:
          - branchMatch: ^feature/.*
            targetBranchMatch: ^main$
  template:
    metadata:
      name: 'planning-preview-{{ .branch_slug }}'
      labels:
        devhub.io/env: preview
        devhub.io/service: planning
        devhub.io/branch: '{{ .branch_slug }}'
        devhub.io/pr: '{{ .number }}'
    spec:
      project: devhub
      source:
        repoURL: https://github.com/Yannis-Alouache/devhub-campus.git
        targetRevision: '{{ .branch }}'
        path: services/planning/chart
        helm:
          valueFiles:
            - values-preview.yaml
          parameters:
            - name: image.tag
              value: '{{ .head_short_sha_7 }}'
            - name: ingress.host
              value: 'planning-preview-{{ .branch_slug }}.devhub.local'
      destination:
        server: https://kubernetes.default.svc
        namespace: 'devhub-preview-{{ .branch_slug }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
