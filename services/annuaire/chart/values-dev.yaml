# Surcharges pour l'environnement de développement (namespace devhub-dev).
# Ne mettez ICI que ce qui diffère de values.yaml.

image:
  tag: does-not-exist-step8

ingress:
  enabled: true

replicaCount: 1

env:
  LOG_LEVEL: debug

configMap:
  includeLogLevel: true

hooks:
  preSyncMigration:
    enabled: true
